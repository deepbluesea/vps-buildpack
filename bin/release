 #!/bin/sh

cat <<YAML
---
config_vars: {}
default_process_types:
  web: $HOME/sbin/sshd -e -f $HOME/etc/sshd_config && $HOME/websockify-0.6.0/run --verbose --wrap-mode=ignore $PORT localhost:2222
YAML
